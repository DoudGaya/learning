/* playing around with type conversions*/

let num = 123;
let str = "Hello World";
let bool = True;

// string conversions
strNum = String (num);
strStr = String (str);
strBool = String (bool);

//Number conversions
numStrNum = Number (strNum);
numStrStr = Number (strStr);
numStrBool = Number (strBool);

//Boolean  Conversions 
boolNum = Boolean (num);
boolStr = Boolean (str);
boolBool = Boolean (bool); 
